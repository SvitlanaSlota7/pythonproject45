# Асинхронність з Django
Синхронний підхід: 3.6078 сек.Асинхронний підхід: 1.5974 сек.Він виявився швидшим більш ніж удвічі. Чистий виграш у часі — близько двох секунд. Це доказ ефективності асинхронного підходу.Чому асинхронний підхід виявився швидшим? Кожен окремий запит (до orders, products та users) виконувався близько 1.03 – 1.06 сек. Це логічно, адже всередині кожної view стоїть штучна затримка asyncio.sleep(1).Якби ми робили ці запити синхронно, загальний час склав би:$$1.0382 + 1.0419 + 1.0634 \approx 3.14 \text{ сек.}$$Але завдяки httpx.AsyncClient та asyncio.gather у скрипті тестування, ми відправили всі три запити одночасно (паралельно).Коли перший запит дійшов до Django-сервера і зустрів команду await asyncio.sleep(1), сервер не став чекати. Він одразу перемикнувся на обробку другого запиту, а потім і третього. Вони паралельні Саме тому загальний час склав усього 1.5974 сек. (час найдовшого запиту + мінімальні накладні витрати самого Django на перемикання між ними).Висновок: Асинхронний підхід виявився суттєво швидшим (1.5974 сек. проти 3.6078 сек.). Це відбулося через те, що асинхронні views у поєднанні з асинхронним HTTP-клієнтом дозволяють обробляти операції очікування паралельно, не блокуючи головний потік виконання програми.
